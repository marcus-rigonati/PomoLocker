packaging==24.2
setuptools==78.1.0

# PyInstaller drives build.sh on every platform; pillow only exists to convert
# the .png icon into a Windows .ico at build time.
PySide6-Essentials==6.11.2
pyinstaller==6.21.0
pillow==11.3.0; sys_platform == "win32"
